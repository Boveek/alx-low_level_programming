#include "main.h"
/**
 * add - Entry point
 * @j: integer
 * @i: integer
 * Return: Result of the addition
 */
int mod(int i, int j)
{
        int modulus_result = i % j;

        return (modulus_result);
}

#include "main.h"
/**
 * mul - writes the sum of an integer
 * @a: The character to add
 * @b: The character to add
 * Return: On success 1.
 */
int mul(int a, int b)
{
	int c = a * b;

	return (c);
}
